import { ValidarDescPipe } from './validar-desc.pipe';

describe('ValidarDescPipe', () => {
  it('create an instance', () => {
    const pipe = new ValidarDescPipe();
    expect(pipe).toBeTruthy();
  });
});
